import React, {Component} from 'react';
import ListViewInfos from '@components/ListViewInfos';

class Home extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return <ListViewInfos/>
  }
}

export default Home;